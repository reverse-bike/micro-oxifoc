# Test code may unwrap, panic, and debug freely. Production code keeps the
# fail-fast lints enabled by the crate manifests.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
allow-dbg-in-tests = true
